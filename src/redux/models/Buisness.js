import { reducerActions as reducers } from "./reducers";

const initialState = {
    buisness: [],
}

export const BuisnessDeails = {
    name: 'BuisnessDeails',
    state: initialState,
    reducers,
}